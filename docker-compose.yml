services:
  api:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    ports: ["8010:8000"]
  ui:
    build: .
    command: streamlit run app/ui.py --server.address 0.0.0.0 --server.port 8510
    ports: ["8510:8510"]
    depends_on: [api]
